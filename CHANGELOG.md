# 변경 이력

이 파일은 무엇이 바뀌었는지보다 **왜 그렇게 했는지**를 남깁니다.
숫자와 결정 근거는 커밋 메시지와 같은 것을 씁니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

---

## [미출시]

### 변경 (세계)

- **영상 스크럽 세계를 2D 픽셀 여정으로 바꿨습니다.** 스크롤이 카메라의 재생 위치가 아니라
  **아잉의 걸음**입니다. 다섯 장소를 순간이동 없이 걸어서 지나갑니다. 계약은
  `docs/pixel-journey.md`, 엔진은 `lib/pixel-journey.js`(`lib/scrub-engine.js` 468줄을 대체,
  195줄). 레이어 속도는 하늘 0.2 · 배경 0.6 · 지면/아잉 1.0 · 전경 소품 1.15.
- **글을 붙잡는 것이 JS가 아니라 CSS입니다.** 다섯 씬은 이제 문서 흐름에 있는 `<section>`이고
  카피는 그 안에서 `position: sticky`로 섭니다. 엔진이 하던 스크롤→불투명도 계산, 라우트 dot,
  힌트, 파티클, 주입 CSS가 통째로 사라졌습니다. 그래서 엔진 청크가 없어도, 모션을 줄여 달라고
  해도 다섯 장은 그대로 읽힙니다 — 서버가 그린 마크업 그 자체가 화면이니까요.
- **씬 사이 이동은 앵커입니다.** `#genesis` … `#vault`는 이제 실제 스크롤 위치를 가진 섹션이라
  JSON-LD가 가리키던 주소가 처음으로 진짜 목적지가 되었습니다. 레일은 `<a href>` 다섯 개 —
  JS 없이도, 키보드로도 갑니다.
- 아잉의 현재 장면을 `.sw-route__dot.is-active`에서 읽던 `useStage`의 특례를 지웠습니다.
  씬이 `[data-stage]` 블록이 되면서 나머지 섹션과 같은 규칙으로 잡힙니다(MutationObserver도 제거).
- 지면은 `background-position`이 아니라 `transform`으로 흐릅니다. 매 프레임 띠 전체를 다시 칠하면
  스크롤 중 프레임이 실제로 죽습니다 — 한 번 칠하고 미는 쪽은 합성기가 공짜로 해 줍니다.
- 이웃한 배경 두 장은 타일 하나만큼 겹치고 뒤 장의 왼쪽 끝만 마스크로 녹입니다. 새벽에서
  해질녘까지 하늘색이 다르므로, 겹치지 않으면 장소가 바뀔 때마다 세로줄이 그어집니다.
- 영상(`public/assets/vid/`, 25MB)과 씬 webp는 **지우지 않고 참조만 끊었습니다.**

### 추가

- **위키 14 → 25조각.** 별도 저장소의 Git 업적 분석(2026-07-21, 세 저장소 전 ref 625개 재집계)과
  실제 저장소 확인으로 근거가 잡힌 것만 옮겼습니다. 새 조각은 백엔드 멀티모듈 재설계와 JDK 21 이관,
  팀 에이전트 스킬 CLI와 PR 자동 리뷰, LLM 번역 파이프라인, 머천다이징·리뷰 진열, QR 장바구니 복구,
  프로모션 역할 분리, Carry·Trade·OCR, 운영 경계(Redis·메트릭·배포 계약), AI를 쓰는 방식입니다.
- 커밋 수는 **두 벌을 그대로 둡니다.** 07-02 분석(main 기준·merge 포함, 5,240)과 07-21 분석(전 ref·non-merge,
  4,280)은 기준이 다를 뿐 어느 쪽도 틀리지 않습니다. 한쪽을 지우면 다른 문서와 어긋나므로 병기하고
  기준 차이를 조각 안에 적었습니다.
- 검색 STOP 목록에 `새로`·`사람`·`방식` 등 7개. 조각이 스무 개를 넘기자 어느 글에나 나올 법한 말이
  어쩌다 한 조각에만 있다는 이유로 희소어 취급을 받아 1순위를 가져갔습니다
  ("쿠폰 왜 새로 만들었어" → 팀 조각). 검증 케이스도 12개 늘려 56건입니다.

### 변경

- **브라우저 모델을 묻지 않고 받습니다.** 헤더의 '켜기' 버튼과 `localStorage` 동의를
  둘 다 뺐습니다. 방문자가 답을 받으려고 창을 열었을 때 570MB 다운로드가 그제서야
  시작되면, 눌러야 하는 버튼이 하나 더 있었을 뿐 기다림은 그대로였습니다. 대신 진입
  즉시 시작해 창을 열 때쯤이면 끝나 있게 했습니다.
- 자동으로 바뀐 만큼 **안 받아야 할 곳**을 가드로 막습니다. Ollama 탐지가 끝난 뒤에만,
  그리고 `navigator.gpu`가 있을 때만 시작합니다. WebGPU 세션은 파일을 다 받은 뒤에야
  만들어지므로, 가드가 없으면 실행할 수도 없는 570MB를 끝까지 받고 나서 죽습니다.

### 제거

- 커버 우상단의 낙관(`之印`)과 상단바 브랜드의 `之印`. 스타일(`.cover__seal`)까지 같이
  뺐습니다.
- 켜기 버튼이 사라지면서 아무도 부르지 않게 된 `sizeLabel`·`findById`.

---

## [2.0.0] — 2026-08-11

Vite SPA를 **Next.js 16.3 App Router**로 옮기고, AI 에이전트가 JS 없이 읽을 수 있는
표면을 만든 릴리스.

계기는 성능이 아니라 **구멍**이었습니다. 이 사이트에서 가장 할 말이 많은 부분 — 다섯 섬의
제목·본문·태그 — 은 스크럽 엔진이 런타임 `innerHTML`로 만들고 있었고, 캐릭터 킷은
하이드레이션 뒤 `fetch`로 그렸습니다. 즉 **JS를 돌리지 않는 크롤러에게 이 이력서는 거의
비어 있었습니다.** 이관의 본론은 그 글을 서버로 옮긴 것입니다.

### 추가

- **AEO 응답 레이어**
  - `Accept: text/markdown`으로 홈을 요청하면 같은 내용의 Clean Markdown(~16KB)을 냅니다.
    리다이렉트가 아니라 **rewrite**라서 URL이 그대로입니다 — 에이전트가 인용하는 주소와
    사람이 여는 주소가 같아야 하기 때문입니다. `Vary: Accept`도 같이 냅니다. (`proxy.js`)
  - `/iron.md` · `/llms.txt` · `/llms-full.txt` — 전부 `lib/markdown.js`가 페이지와
    **같은 상수**에서 생성합니다. 따로 관리하는 사본이 없으니 화면과 어긋날 수 없습니다.
  - `robots.txt`에 GPTBot·ClaudeBot·PerplexityBot 등 **16종을 이름으로** 허용했습니다.
    와일드카드 `Allow`만 두면 그것을 "학습 거부 안 함"으로만 읽고 색인은 건너뛰는 봇이 있습니다.
- **JSON-LD** `@graph` — `ProfilePage` · `Person` · `ItemList`(기여 사례/저장소) · `FAQPage`.
  규칙 하나를 지켰습니다: **화면에 실제로 보이는 글만 올린다.** 위키 16조각은 화면에
  없으므로 FAQ로 올리지 않고 마크다운 레이어로만 냅니다. 보이지 않는 답을 구조화 데이터에
  넣는 건 그냥 스팸입니다.
- **Metadata API** — `metadataBase`, canonical, OpenGraph(profile), Twitter,
  `alternates.types`로 마크다운 표면 광고. `robots.js` / `sitemap.js`로 생성.
- `lib/site.js` — 사이트 URL을 하드코딩하지 않습니다. `NEXT_PUBLIC_SITE_URL` →
  Vercel의 `VERCEL_PROJECT_PRODUCTION_URL` 순으로 잡습니다. 프리뷰 배포가 자기를
  정본이라 주장하면 크롤러가 그쪽을 인덱싱합니다.

### 변경

- **월드 카피를 서버가 렌더합니다.** 엔진은 `.sw-copylayer`가 이미 있으면 **그 마크업을
  집어 씁니다**(`lib/scrub-engine.js`). 없으면 예전처럼 만듭니다 — DOM에 두 벌이
  생기지 않고, 엔진을 다른 데서 단독으로 쓰던 방식도 그대로 삽니다.
- **캐릭터 킷이 서버 컴포넌트가 됐습니다.** `aing-kit.json`은 저장소 안의 정적 파일이므로
  빌드 타임에 import합니다. `fetch` 폭포와 "아직 안 온 상태"가 통째로 없어졌습니다.
- **Footprint의 IntersectionObserver를 CSS `animation-timeline: view()`로 대체.**
  막대 하나 차오르게 하려고 섹션 전체가 클라이언트 컴포넌트였습니다. 지원하지 않는
  브라우저는 막대가 곧바로 제 길이로 찹니다 — **수치는 언제나 정확합니다.**
- **오라클 모델: Gemma 4 E2B 3.4GB → Qwen3 0.6B 570MB** (q4f16 실측, 6배).
  이 모델이 하는 일은 지식을 꺼내는 게 아니라 *검색된 위키 문단을 3문장으로 다듬는 것*이라,
  파라미터보다 기다림이 먼저 체감됩니다. 방문자는 570MB / 1.4GB 중에 고릅니다.
  Gemma 4는 멀티모달(`AutoProcessor`)이라 뺐습니다 — 텍스트 한 갈래를 위해 로더를
  두 벌 지고 갈 이유가 없습니다. 동의는 모델 id로 저장해 재방문자가 고르지도 않은
  모델을 받지 않습니다. (`components/oracle/models.js`)
- 라우트 dot의 `aria-label`을 엔진이 직접 답니다. 마운트 뒤 밖에서 덧칠하던 코드 삭제.
- `src/` → `app/` · `components/` · `lib/`. 모든 이동은 `git mv`라 이력이 이어집니다.

### 삭제

- `index.html` · `vite.config.js` · `src/main.jsx` — `main.jsx`의 sky URL 절대화 해킹은
  base가 `/`가 되면서 CSS 한 줄(`--sky-img`)로 충분해졌습니다. 첫 페인트 전 스크립트가 사라집니다.
- `public/llms.txt` · `robots.txt` · `sitemap.xml` — 손으로 관리하던 사본. 이제 생성합니다.
  (`public/`의 정적 파일은 같은 이름의 라우트를 가리므로 지우지 않으면 새 것이 안 나옵니다.)
- `public/ai-ng-favicon.png` **1.5MB** — 1024px 원본이 파비콘으로 그대로 들어가 있었습니다.
  `app/icon.png` 192px **52KB**로 교체.

### 고침 (코드 리뷰 대응)

리뷰가 **"동작한다고 적어 둔 것이 동작하지 않는"** 두 건을 잡았습니다. 둘 다 실측했습니다.

- **프록시 matcher가 통째로 무시되고 있었습니다.** Next 16은 파일명과 함수명을 `proxy`로
  바꿨지만 설정 export는 여전히 `config`입니다 — `proxyConfig`라는 문자열은 Next 안에
  존재하지 않습니다. 그래서 프록시가 모든 요청에 걸렸고, `Accept: text/markdown`으로
  `/llms.txt`를 부르면 llms.txt가 아니라 이력서 전문 16KB가 돌아왔습니다.
- **저장소 막대가 한 번도 차오르지 않았습니다.** `.repo__meter`의 `overflow: hidden`이
  그 상자를 스크롤 컨테이너로 만들어, `view()` 타임라인이 뷰포트가 아니라 **거기에**
  묶였습니다. 그 상자는 스크롤될 일이 없으니 진행률이 1에 붙박여 막대는 늘 가득 차 있었고,
  즉 IntersectionObserver를 걷어내면서 연출을 통째로 잃은 상태였습니다.
  6개 지점 실측: 수정 전 `progress === 1` / `416px` 고정 → 수정 후
  `0px(0) → 91px(.05) → 409px(.70) → 416px(1)`.
- `lib/site.js` — URL을 못 찾은 CI 빌드에서 경고합니다. 로컬 빌드에서까지 울리면 무뎌집니다.
- `app/robots.js` — `Host` 지시어는 스킴 없는 호스트명입니다.
- `Oracle.jsx` — morph 임포트가 `mounted` 게이트와 경합했습니다. 청크가 마이크로태스크로
  풀리면 canvas·panel ref가 둘 다 null이라 `broken`도 못 세우고 조용히 죽습니다
  (deps가 `[]`라 재시도도 없음). 지금은 네트워크 왕복 덕에 우연히 안 걸릴 뿐이었습니다.
- `llm.worker.js` — `ask()`가 `load()`를 인자 없이 불러 방문자가 고르지 않은 모델을
  집을 수 있었습니다.
- `Guide.jsx` — SSR이 `still=false`로 굳어 **reduced-motion 사용자가 하이드레이션 전에
  애니메이션 루프를 내려받고 봤습니다.** 기본을 정지 포즈로 뒤집었습니다.
- `lib/wiki.js` — 이번 변경이 철회한 수치(74KB, Lighthouse 100)를 위키가 계속 퍼뜨리고
  있었습니다. 마크다운 레이어가 이 글을 그대로 싣고 오라클이 답으로 인용하므로,
  **기계용 이력서가 사람용이 부인한 숫자를 주장하는** 상태였습니다.

### 고침 (코드 리뷰 2차)

- **3D 뷰어의 WebGL 폴백은 한 번도 동작한 적이 없었습니다.** `three/webgpu`는 재수출
  목록에 `WebGLRenderer`가 없습니다(RenderTarget들만 있습니다). 그래서 `navigator.gpu`는
  있는데 `init()`이 실패하는 기기 — 드라이버 블록리스트, 플래그로 끈 경우 — 에서
  `new undefined(...)`가 되어, 폴백이 아니라 에러 화면이 떴습니다. 그 경로에서만 코어
  빌드를 마저 받아 진짜로 폴백합니다.
- **아잉의 설명이 조용히 어긋날 수 있었습니다.** `useStage`는 rAF 한 번 뒤에 `.sw-route`를
  찾아 MutationObserver를 붙였는데, 엔진이 동적 import가 되면서 그 레일은 청크를 받기 전엔
  없습니다 — 콜드 로드에서 네트워크 왕복이 한 프레임을 이길 수 없습니다. 서버가 그리는
  `#world`를 보게 해서 경합을 없앴습니다. (따뜻한 캐시에서는 통과해 1차 리뷰를 빠져나갔습니다.)
- **워커가 죽으면 입력창이 새로고침 전까지 잠겼습니다.** `ask`의 프라미스는 `done`/`error`
  메시지로만 풀리는데, WebGPU OOM이나 디바이스 로스트에서는 둘 다 안 옵니다. 타임아웃과
  `onerror`의 pending 정리를 붙였습니다. Ollama 갈래엔 이미 있던 가드가 정작 방문자에게
  나가는 갈래엔 없었습니다.
- **3D 뷰어가 1.22MB 풀 모델을 받고 있었습니다.** 205kB짜리 `aing-lite.glb`는 배포에만
  실리고 아무도 안 썼습니다. 뷰어는 미리보기이므로 lite(실측 191kB 전송)로, 풀 모델은
  킷 다운로드에 남깁니다.
- **오라클 헤더에 모델 버튼을 둘 넣은 게 과했습니다.** 390px에서 헤더가 58 → 106px로
  부풀어 판의 절반이 모델 고르기가 됐습니다. 버튼은 기본 모델 하나로 되돌리고(다른 모델을
  쓰려면 `models.js` 순서를 바꾸면 됩니다), 긴 라벨이 판 밖으로 새지 않게 `flex-wrap`을 뒀습니다.
- 저장된 동의는 **정확히 일치할 때만** 되살립니다. 목록에서 사라진 모델(이번에 뺀 Gemma 4)을
  허락해 둔 사람이 묻지도 않은 모델을 내려받던 경로였습니다.
- `localStorage`를 try/catch로 감쌌습니다. Safari 시크릿 모드에서는 접근 자체가 던져서
  '켜기'를 눌러도 스피너조차 안 떴습니다.
- Live3D의 `dispose`를 렌더러 생성 직후에 겁니다. GLB를 받는 동안 언마운트되면 GPU
  컨텍스트와 캔버스가 샜습니다(브라우저 예산 8~16개).
- `WorldMount`의 `catch`를 import에만 겁니다. 마운트 중 예외까지 "엔진을 못 받았다"로
  처리되면, 반쯤 지어진 세계 위로 카피가 쏟아지고 재진입 가드 때문에 재시도도 막혔습니다.
- JSON-LD에서 `<`를 이스케이프합니다. 본문에 `<` 하나만 들어가도(예: `p95 <200ms`)
  HTML 파서가 스크립트를 일찍 닫습니다.
- `/llms-full.txt`가 `/iron.md`를 정본으로 가리키는 `Link: rel="canonical"`을 냅니다.
  같은 글이 두 URL에 있는데 신호가 없으면, 단일 소스로 만든 레이어가 중복 콘텐츠로 갈라집니다.
- CI 빌드에서 사이트 URL이 없으면 **경고가 아니라 세웁니다.** 초록색 로그를 스쳐 가는
  경고는 가드가 아닙니다.
- Pretendard를 `media="print"` + body 끝 인라인 스크립트로 되돌려 논블로킹으로 받습니다.
  RSC에서 문자열 `onload`를 못 쓴다고 렌더 블로킹을 받아들일 일은 아니었습니다.

**고치지 못한 것 하나.** 협상하는 URL은 두 갈래 모두 `Vary: Accept`를 내야 하는데,
HTML 갈래에는 붙일 방법이 없습니다 — Next이 App Router 페이지 응답의 Vary를 자기 RSC
목록으로 덮어씁니다. `NextResponse.next()`, 같은 경로 rewrite, `next.config`의 `headers()`
(프록시를 아예 안 태워도) 셋 다 실측으로 확인했습니다. 그래서 협상은 편의 기능으로 두고,
에이전트에게 약속하는 정본 경로는 몸이 하나뿐인 `/iron.md`입니다 — `link rel="alternate"`,
llms.txt, sitemap 셋 다 그 주소를 가리킵니다.

### 알려진 비용

- **초기 번들 74KB → 185KB gzip.** App Router 런타임의 값입니다. 스크럽 엔진·파티클 막·
  three.js·모델은 전부 필요할 때만 받지만, 런타임 자체는 줄일 수 없습니다.
  에이전트에게는 초기 JS가 0이므로 이 거래는 그쪽에서 이득이고, 사람 쪽에서는
  LCP를 하늘 이미지와 폰트가 지배하므로 체감이 크지 않습니다.
- Lighthouse 수치는 Vercel 배포 후 재측정해야 의미가 있어 README에서 내렸습니다.
- Pretendard 동적 서브셋을 논블로킹으로 받던 `preload`+`onload` 트릭은 문자열 이벤트
  핸들러가 필요해 RSC에서 못 씁니다. preconnect된 CDN의 CSS 한 장으로 되돌렸습니다.

---

## [1.1.0] — 2026-08-10

검색창 하나로 이력을 물어볼 수 있게 만든 릴리스. 화면 한가운데의 유리판이
**그 자리에서 자라 채팅창이 됩니다** — 새 창도, 페이지 이동도 없습니다.

### 추가

- **오라클 검색창** (`src/oracle/`)
  - 파티클 16,000개가 rounded-rect 둘레를 목표로 모여 막을 만듭니다.
    위치·속도 계산은 전부 WebGPU 컴퓨트 셰이더(WGSL)가 합니다.
  - DOM 판은 항상 확장 크기로 고정하고 `clip-path`만 바꿉니다 — 리플로 0.
  - 판 외곽이 거의 완성된 뒤(`morph` 0.72~1.0)에야 헤더 → 로그 → 입력이 차례로 맺힙니다.
  - 플레이스홀더 문구가 신기루처럼 글자 단위로 맺혔다 증발합니다.
  - `/` 또는 `Cmd+K`로 어디서든 열고 `Esc`로 접습니다.
- **답변 엔진 3단 폴백** (`src/oracle/brain.js`)

  | 순위 | 엔진 | 조건 |
  |:-:|---|---|
  | 1 | Ollama | 이 컴퓨터에서 돌고 있을 때 (localhost에서만 탐지) |
  | 2 | Gemma 4 | `onnx-community/gemma-4-E2B-it-ONNX` q4f16을 WebGPU로. 최초 1회 3.4GB, 버튼을 눌러야만 |
  | 3 | 위키 | 모델이 없어도 조각을 그대로 인용 |

  어느 쪽이든 질문은 브라우저 밖으로 나가지 않습니다.
- **이력 위키** (`src/oracle/wiki.js`) — 16개 조각. PAR 이력서(`ZIVO/docs/resume/iron-resume-par.md`)의
  Problem·Analyze·Action·Result를 옮겨, 무엇을 했는지뿐 아니라 **어떤 선택지를 왜 골랐는지**까지 답합니다.
- **사실 정확성 가드레일** — "하지 않은 일" 조각을 따로 뒀습니다. RAG/pgvector는 계획 흔적만 있고
  구현이 없으며, 백엔드 결제 코어는 다른 기여자 소유입니다. 모델이 이 선을 넘지 않게 근거에 명시했습니다.
- `npm run check` — 44개 체크. 검색이 엉뚱한 조각을 1순위에 올리면 실패합니다.

### 변경

- 커버를 3행 그리드(`1fr auto 1fr auto`)로 바꿔 검색창을 화면 한복판에 고정했습니다.
  `.cover__stage`에 `height: 100%`가 핵심입니다 — 높이가 미확정이면 `fr` 행은 남는 공간을 나누는 대신
  **서로 크기를 맞추려 들어**, 이름 판 높이가 아래 행에 복사되면서 커버가 뷰포트의 1.5배로 늘어납니다.
- 커버 본문을 한 문장 줄이고 여백을 재분배해 그 자리를 만들었습니다.

### 성능

- 오라클 모듈(18.6KB / gzip 7.3KB)은 첫 상호작용 전까지 로드되지만, `@huggingface/transformers`(498KB)는
  **워커 청크로 분리**돼 모델을 켜지 않으면 내려받지 않습니다. 초기 번들 영향은 gzip 기준 +6KB.
- 커버를 벗어나면 파티클 렌더를 멈추고 캔버스를 페이드아웃합니다. 안 그러면 섬을 보는 내내
  막이 화면 중앙에 떠 있고, 스크럽하는 영상과 GPU를 나눠 쓰느라 프레임을 잃습니다.
- 프레임이 계속 45fps 아래로 떨어지면 파티클 수를 0.72배씩 줄입니다.

### 접근성

- 열려 있는 동안 `#root`를 `inert`로 재우고 스크롤을 잠급니다. 판은 portal로 `body`에 있으므로
  뒤 페이지만 정확히 차단됩니다(탭·스크린리더 모두).
- 막이 다 맺힌 뒤에 입력칸으로 포커스를 옮깁니다 — 그 전에는 입력칸이 아직 잘려 있습니다.
- `prefers-reduced-motion`에서는 신기루 애니메이션을 걷어냅니다. 전역 규칙이 모든 애니메이션을
  `0.01ms`로 줄이는데, 그러면 신기루가 '증발한' 끝 상태로 굳어 글자가 아예 사라집니다.

### 수정

- 검색 순위 세 가지를 고쳤습니다.
  - 이름(`iron`/`박상욱`)을 뺀 질문으로 먼저 찾습니다. 위키 전체가 한 사람 이야기라
    이름은 주제를 못 가르는데, 없으면 이름만 들어가도 소개 조각이 1등을 합니다.
  - 2-gram을 한글에만 적용합니다. 한글은 두 글자가 의미 단위지만 영문 두 글자는 아무 뜻도 없어서,
    `archunit`의 `ch`가 `opensearch`에 걸리는 식으로 노이즈만 만듭니다.
  - 길이 정규화를 넣되 완만하게(BM25의 `b`항). 안 나누면 개요 조각이 모든 질문을 먹고,
    제곱근으로 나누면 반대로 가장 짧은 조각이 아무 질문에나 튀어나옵니다.
  - "설계 / 구현 / 어떻게" 같은 일반 서술어를 불용어로 뺐습니다. 안 빼면 "설계"가 어쩌다
    한 제목에만 있다는 이유로 희소어 취급을 받아, "쿠폰은 왜 새로 설계했어?"에 결제 조각이 1등을 합니다.
  - 세부 조각을 거느린 개요(백엔드)에는 양보 계수를 줬습니다 — 개요는 세부의 주제어를 전부 품고 있습니다.
  - 기술명을 한글로 물어도(스프링부트, 리액트) 걸리게 태그를 보강했습니다.
- WebGPU `uncapturederror` 리스너를 붙였습니다. 검증 오류는 예외로 오지 않아서,
  안 잡으면 화면만 비고 원인을 볼 수 없습니다.
- 좁은 화면에서 접힌 폭과 펼친 폭이 둘 다 화면에 걸려 "자란다"는 느낌이 사라지던 것을 고쳤습니다.

### 검증

Playwright(Apple Metal, headless)에서 확인한 것:

- 접힘: 막이 화면 세로 42~56% 띠(= 검색창 둘레)에 맺힘
- 펼침: 19~79% × 18~80%의 사각 테두리(= 채팅창 둘레)로 자라고 `clip`은 `inset(0)`까지 감
- 390×812에서도 판이 화면 안에 들어오고, 커버가 비워둔 자리와 8px 안에서 일치

> WebGPU 캔버스는 프레임 표시 후 텍스처가 detach되므로, 늦게 `drawImage`하면 빈 값이 나옵니다.
> 측정은 **렌더와 같은 프레임에서** 해야 합니다.

미검증으로 남은 것: 실제 GPU에서의 시각 품질(headless는 2fps라 스크린샷이 타임아웃),
Gemma 4의 실제 추론(3.4GB라 동의 없이 받지 않음), Ollama 경로(이 머신의 `gemma4:e4b` 런너가 세그폴트).

### 수정 (코드 리뷰 후속)

- **커버가 뷰포트의 1.43배로 부풀고 검색창이 hero 위에 겹쳤습니다.** 위에 적은 `height:100%`는
  실제로 선언되지 않았고, 넣어도 고쳐지지 않았습니다 — 부모가 `min-height`만 가지면 백분율 높이가
  기댈 기준이 없기 때문입니다. `.cover`에 `height:100svh`를 명시하고, 상하 패딩을 대칭으로
  되돌리고(비대칭이면 그만큼 자리가 위로 밀립니다), 스크롤 큐를 흐름에서 빼서 세 행이
  `[1fr][자리][1fr]`이 되게 했습니다. 1440×900 · 1024×768 · 390×812 · 360×640 모두 어긋남 0px,
  겹침 없음, 커버 1.00×. 낮은 화면(<720px)에서는 본문 한 줄을 접어서라도 한 화면에 넣습니다.
- **조사가 붙은 영문 기술명이 통째로 검색 실패했습니다.** "ArchUnit을", "iron은?", "Outbox를"이
  전부 빈 결과라 모델에 닿지도 못하고 "위키에 없습니다"로 답했습니다. `norm`이 라틴+한글을 한
  토큰으로 두는 바람에 한글이 섞였다는 이유로 라틴 2-gram이 생겼는데, 태그 쪽 `archunit`은 순수
  라틴이라 2-gram이 없어 영영 만나지 못합니다. 경계에서 토큰을 쪼개 해결했습니다.
- 파티클 장은 비동기로 붙는데 일시정지 이펙트가 `[away, open]`에만 걸려 있어, 스크롤이 복원된 채
  뜬 페이지는 막이 영영 안 재워졌습니다.
- Ollama 탐지가 끝나기 전에 자동 동의가 발동해, Ollama가 도는 컴퓨터에서도 재방문자가 쓰지도 않을
  3.4GB를 받았습니다.
- 스트리밍 도중 엔진이 죽으면 반토막 문장이 완성된 답처럼 남았습니다.
- 모델 로드 실패가 막다른 길이었습니다 — 헤더는 "위키 인용"이라 거짓말하고 재시도 버튼도
  사라졌습니다. `error` 상태 표시와 다시 시도, `worker.onerror`를 넣었습니다.
- Ollama `/api/chat`에 타임아웃이 없어 런너가 멎으면 입력창이 영영 잠겼습니다(60초 제한).
- `pointerleave`를 `window`에 걸어서 (버블하지 않으므로) 영영 오지 않았습니다 — 커서가 창을 떠나도
  파티클이 마지막 좌표를 따라다녔습니다.
- `Backend` 생성자가 던지면 에러 스코프도 GPU 디바이스도 놓지 않았습니다.
- morph 청크 로드 실패가 조용했습니다 — 눌러도 아무 일도 일어나지 않는 판이 됐습니다.
- 토큰마다 smooth 스크롤을 새로 걸어 로그가 떨리고, `aria-live`가 답을 처음부터 반복 낭독했습니다.

---

## [1.0.0] — 2026-08-10

스크롤이 카메라를 움직이는 이력서 사이트.

### 추가

- 스크럽 엔진 — 미리 렌더한 카메라 비행 클립을 스크롤 위치로 스크럽. 의존성 0의 바닐라 JS,
  클립을 Blob으로 로드해 호스트의 byte-range 지원과 무관하게 seek 보장
- 다섯 개의 섬(기원·검증·규율·지능·정합성)과 이음매 없는 커넥터
- Ai-ng 캐릭터 킷 — 표정 16종, 액션 16종, 모션 6종, GLB 3D 모델, 엔진용 아틀라스와 매니페스트
- 저장소 기여도·일하는 방식·콜로폰 섹션

### 성능

- 초기 번들 74KB gzip (three.js는 킷 섹션이 뷰포트에 들어온 뒤에만 로드)
- 3D 원본 54.44MB → 1.19MB (simplify + meshopt + 텍스처 WebP)
- Lighthouse(mobile) 접근성·베스트프랙티스·SEO·에이전틱 모두 100, LCP 342ms, CLS 0.00
